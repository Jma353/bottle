# todo playground

A minimal todo app that demonstrates basic `bottle` usage with React and MobX.

## What it shows

- Creating a `Collection<Todo>` without sync callbacks
- Adding todos with `collection.create`
- Toggling todos with `collection.update`
- Rendering reactively with `mobx-react-lite`

## Running

```sh
pnpm dev
```
